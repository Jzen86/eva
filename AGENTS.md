# Eva — рабочие правила для агента

## Что это

Telegram-компаньон. Форк/выжимка из `Aimagine-life/Betsy` (MIT). Апстрим мёртв, не синкаемся.
Источник истины по состоянию проекта — этот файл плюс `FORK-PLAN.md`.

## Железные правила

1. **Данные и секреты — не в репозитории.** Только код. `config.example.yaml` с заглушками.
   Реальный `config.yaml`, `*.db`, `.env` — в `.gitignore`, живут в `~/.eva/`.
   Никогда не коммитить ключи, токены, пароли, реальные значения конфига.
2. **Секреты не светятся в чат.** Только путь, имя переменной, длина значения.
3. **Никаких правок на сервере.** Сервер `rdp-fi` — только рантайм. Разработка здесь.
4. **`/study` не трогать** — это метрика, а не фича.
5. **Апстрим не форкаем.** Правки идут в этот репозиторий.

## Проверка перед коммитом

```bash
npm run typecheck   # должно быть 0 ошибок
npm run build       # dist/index.js
npm test            # 33 файла, зелёные
```

Тесты гоняются на любой машине: `vitest.config.ts` подменяет `better-sqlite3`
на шим поверх `node:sqlite` (`test/shim/better-sqlite3.ts`). Нативный модуль
здесь не собирается, и из-за него падали 12 файлов с базой — они прятали
настоящие баги за «средовой» ошибкой. Не убирай алиас, не проверив, что тесты
всё ещё проходят.

## Что где

| Что | Где |
|---|---|
| Провайдеры LLM | `src/core/llm/registry.ts` |
| Классификация ошибок | `src/core/llm/errors.ts` |
| OpenAI-совместимый клиент | `src/core/llm/providers/openai-compat.ts` |
| Роутер и фолбэки | `src/core/llm/router.ts` |
| Конфиг | `src/core/config.ts` |
| Память и БД | `src/core/memory/` |
| Самообучение | `src/core/memory/study-runner.ts` |
| Самодиагностика | `src/core/doctor.ts` (инструмент `doctor`, CLI `eva doctor`) |
| Гейт подтверждения правок | `src/core/pending.ts` (`/yes`, `/no`, `/pending`) |
| Установка | `src/core/init.ts` (CLI `eva init`) |
| **Фото личности** | `src/core/reference-photo.ts` — путь, копирование, проверка. Единственное место, которое знает, где фото |
| **Конструктор личности** | `src/core/persona-quest.ts` — четыре вопроса, чистая машина состояний. Проводка в `src/channels/telegram/handlers.ts` (`/persona`, `/cancel`) |
| **Состав тулз** | `src/core/toolsets.ts` — **чистая функция конфига**, не `tools.register(...)` в `main()` |
| **Что можно выполнить** | `src/core/shell-policy.ts` — разбор командной строки и вердикт «пустить / спросить» |
| Тулзы агенту | `src/core/tools/` |
| Telegram | `src/channels/telegram/` |
| Точка входа и CLI | `src/index.ts` |

## Известные особенности

- **Репозиторий — база, а не экземпляр.** Сюда не попадают ключи, база, личность,
  имя владельца и серверные пути. `init` ставит её **как машину**: токен,
  провайдер, модель, имя — и больше ничего (писал характер, пол и правило
  общения — и это было неверно: дефолт, который человек, — это человек,
  которого владелец не выбирал). Личность собирается в чате: `/persona`. Не
  возвращай в код ни промт, ни имя, ни фото, ни «пример настоящего конфига
  этого сервера». Всё личное — в `~/.eva/`, репозиторий обновляется поверх.
- **Личность собирается в чате, а не в `init`.** `src/core/persona-quest.ts` —
  чистая машина состояний (без телеграма, без файлов, часы передаются
  параметром), проводка в `handlers.ts`. Причины, которые не даётся забыть:
  фото лежит на телефоне, а не на сервере; ответы приходят туда же, где и
  вопросы; и «оживить её» должно быть действием, а не галкой в анкете.
  Пропущенный ответ = «не знаю» и **не** повод удалять то, что уже было.
- **Личность читается из файла на каждый запрос.** `Engine.liveConfig()`
  перечитывает `config.yaml` перед сборкой промта, потому что характер, заданный
  в чате, обязан быть в её следующем ответе, а не в ответе после рестарта
  (владелец звал это «потом командами можно поменять», и раньше это работало
  только после перезапуска). Если файл не читается — остаётся снимок на старте.
- **Путь к фото — только через `reference-photo.ts`.** Он выводится из директории
  конфига, а не из `os.homedir()` жёстко: `EVA_CONFIG_PATH` уводит конфиг, и фото
  обязано уйти с ним, иначе конструктор пишет в одно место, а бот ищет в другом.
  Путь фото раньше был захардкожен в пяти файлах. Новый код, которому нужен путь,
  вызывает `referencePhotoPath()`, а не собирает его сам.
- **`security.tools` удалён как мёртвый.** Он дублировал живую секцию `tools:` и
  стоял в `config.example.yaml` с дефолтами `browser/npm_install: true`, то есть
  владелец настраивал то, что не читается **нигде**. Единственные флаги — в
  `tools:`; добавление второго места для того же флага обязано идти через
  тест «это поле кем-то читается».
- **Провайдер — это адрес + ключ.** Не зашивай новые провайдеры в код: они
  добавляются либо в `PROVIDER_PRESETS` (`registry.ts`), либо просто описываются
  в конфиге через `base_url`/`api_key`. Тот же принцип для `image_gen.base_url`.
- `loadConfig()` вызывается один раз на старте. Всё, что меняет конфиг в рантайме,
  обязано достучаться до живого роутера: `registry.onChange()` переписывает
  `config.yaml`, а `router.role(name)` резолвится лениво при каждом запросе.
  Правка файла в обход реестра — молчаливый рассинхрон.
- `self_config` пишет в YAML сразу и синхронно, но живой роутер изменение не
  видит. После него нужен `registry.setRole(...)`, а не запись в файл.
- **Деградация переключается синхронно**, прямо в `noteFailure()`. Не оборачивай
  переходы в промисы: отложенный флип флага приводит к тому, что Ева молча уходит
  на запасную модель и не сообщает об этом. Это уже было багом, тест ловит.
- `onOwnerClaimed` в `index.ts` вызывает `saveConfig` с устаревшим in-memory
  объектом и способен затереть сделанные правки.
- Генерация картинок через `modalities:["image"]` — расширение OpenRouter, не
  часть спецификации OpenAI. Переносить на другой эндпоинт имеет смысл только
  если он это умеет.
- **Индекс памяти считается в JS, SQLite не умеет.** `knowledge.stems` —
  это `indexText(topic + insight)` из `stem-ru.ts`: стемы **плюс** сырые слова.
  Сырые обязательны: «котёнок» ни одним правилом не сводится к «кот», но
  начинается с тех же букв, и префиксный поиск его добирает.
- **Формат индекса версионируется** в таблице `eva_meta`
  (`knowledge_index_version`, сейчас 2). Сменил, что делает `indexText`, —
  поднял константу, и миграция сама перестроит индекс у живого бота.
- **Никогда не пиши в `knowledge_fts` сырым текстом.** Пользовательское
  сообщение идёт в `searchKnowledge` как есть, а FTS5 читает `:` `~` `^` `"`
  `*` как синтаксис. Только через `buildMatchQuery`, который токенизирует и
  кавычит каждый термин.
- `searchKnowledge` глотает ошибки БД и возвращает пустой массив. Поменяешь на
  проброс — обычное сообщение с двоеточием уронит ход.
- **Не правь `knowledge_fts` руками.** Триггеры уже снимают и кладут строку
  обратно при UPDATE. Ручное удаление поверх триггера = двойное удаление =
  `database disk image is malformed` на следующем чтении. Вытеснение — это
  `UPDATE knowledge SET superseded_at`, а отсев делает `WHERE superseded_at IS NULL`
  в самом запросе.
- **Порядок в `rowsToRetire` — не украшение, а смысл.** `ORDER BY access_count DESC,
  timestamp DESC, id DESC` + `OFFSET keep` оставляет используемое и вытесняет
  невостребованное. Обратная сортировка с тем же `OFFSET` вытесняет ровно то,
  что работает, — так и было в первой версии, поймал тест
  `retires an unused memory before one that is in use`.
- **Все записи в память идут через `learnInsight`.** Прямой вызов `addKnowledge`
  обходит дедуп: так она могла записать «люблю грибы» сразу после «не люблю
  грибы», и обе строки оставались.
- `retireKnowledge(id, supersededBy?)` — с `supersededBy` факт **исправлен**,
  без него — вытеснен за место. Разница видна в `superseded_by` и отвечает на
  вопрос «почему Ева когда-то считала иначе».
- **Диагностика — `src/core/doctor.ts`, инструмент `doctor`, CLI `eva doctor`.**
  Проверки чинят то, что молчит: устаревший индекс памяти выглядит как «всё
  забыла», мёртвый провайдер — как «перестала стараться», потерянное поле
  конфига — как «сменилась личность». Секция, которая бросила исключение, не
  роняет отчёт: упавшая проверка и есть диагноз.
- **Считать строки в `knowledge_fts` бесполезно.** Это external-content таблица
  над `knowledge`, поэтому `count(*)` читает content-таблицу и всегда совпадает
  даже при полностью пустом индексе. `integrity-check` FTS5 тоже проходит на
  пустом индексе — пустой индекс целостен. Поэтому `doctor` спрашивает индекс
  поведенчески: берёт несколько свежих строк, достаёт из их `stems` слово и
  просит индекс найти именно эту строку.
- **Проба провайдеров в `doctor` — по запросу (`action=probe`, `--probe`).**
  Это настоящий запрос за деньги; в дешёвом прогоне сетевых вызовов нет.
- **Тулзы — `src/core/toolsets.ts`, и это единственное место, где решается, что
  зарегистрировано.** Не пиши `tools.register(...)` в `main()`: пока список жил
  там, его нельзя было спросить, не поднимая бота, и он протух незаметно —
  `selfie` и `voice` оставались в промпте с пустым ключом, а модель, до них
  дотянувшись, читала ошибку как «сломана», а не «не установлено». У каждого
  тулза есть уровень (`core` / `keyed` / `opt-in`) и, когда его нет, причина.
  `test/core/toolsets.test.ts` держит обещание «голый тулз + один ключ = рабочий
  бот».
- **`browser`, `ssh`, `npm_install` — по флагу `tools:`, не по умолчанию.**
  Произвольное выполнение кода и 150 МБ Chromium — не то, что тащит каждый
  установщик. `BrowserTool` больше не выполняет `npx playwright install` в рантайме
  по факту провала вызова: решение тянуть чужой бинарник принимала модель,
  решившая открыть страницу.
- **Что `shell`/`ssh` вправе выполнить — `src/core/shell-policy.ts`, allowlist.**
  Чёрного списка из четырёх строк («rm -rf /», «mkfs», «dd if=», «format») больше
  нет: он умел только то, о чём кто-то уже подумал, и всё остальное — `sh -c`,
  `find -delete`, `| xargs rm`, `curl -T` — проходило через него целиком. Правило
  теперь обратное: идёт сразу только то, что состоит из бинарников, **не способных
  ничего записать ни в одном режиме**; остальное паркуется через `pending.ts` и
  ждёт `/yes`. `git`, `curl`, `docker`, `npm` в список **не входят даже в
  читающих режимах** — у каждого слишком много пишущих, таблица была бы вымыслом,
  а `/yes` стоит одного тапа.
  Правило для себя: если не могу уверенно сказать, что бинарник не пишет, его там
  нет. Подстановки (`$()`, backticks), перенаправления и `&` ловятся структурно, до
  разбора по бинарникам; кавычки разбираются с учётом кавычек, а несбалансированные
  означают «не знаю» → гейт (`shapeOf` возвращает `null`, а не догадку).
- **Ручка расширения списка называется `tools.shell_trust`, а не `allow`.** Имя в
  ней означает «и пишущие режимы без спроса»: `git` в списке → `git push` идёт
  сам. Это решение о доверии, поэтому `doctor` печатает содержимое списка
  отдельной строкой (`config.tools.trust`, severity `warn`) — решение, сделанное
  один раз, иначе не вспоминают.
- **Один `pending`-предложение на тулзу, и у него есть `registerApprovalApplier`.**
  `requireApproval` сам отказывает, если нет `_userId` (вызов без чата) — свой
  фолбэк в тулзе после него мёртвый код. «Не повторяй попытку» живёт в общем
  сообщении `requireApproval`, а не в описании тулзы: описание модель читает
  заранее, а сообщение — в момент, когда иначе и повторит.
- **Скрытый ввод секретов — raw-режимом, не readline.** Readline печатает введённые
  символы через функцию, связанную с настоящим stdout в момент создания
  интерфейса, так что `rl.output = muted` не скрывает эхо (глушит промпты).
  Литеральный control-байт в исходнике тоже хрупок — пиши `"\u0003"`, иначе
  редактор или diff сделает сравнение с пустой строкой, которое не срабатывает.
- **`doctor` переводит известные поломки установки в действие** (`explainFailure`).
  Прежде всего отсутствие нативной части `better-sqlite3`: на голом Linux без
  `build-essential` это самая частая поломка, а сообщение загрузчика
  («Could not locate the bindings file. Tried:» + 14 путей) не называет команды,
  которая её чинит. На незнакомую ошибку не гадаем — это дороже, чем её
  отсутствие.
